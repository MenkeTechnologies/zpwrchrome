{
  "name": "zpwrchrome",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension — recent-tabs switcher with 48 keyboard commands (incl. UNIX `pass` integration + segmented download manager via native messaging)",
  "author": "MenkeTechnologies",
  "license": "MIT",
  "scripts": {
    "test": "node --test --test-reporter=spec tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
