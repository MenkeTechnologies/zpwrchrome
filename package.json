{
  "name": "zpwrchrome",
  "version": "0.2.4",
  "private": true,
  "type": "module",
  "description": "Chrome extension — recent-tabs switcher with 28 keyboard commands",
  "author": "MenkeTechnologies",
  "license": "MIT",
  "scripts": {
    "test": "node --test --test-reporter=spec tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
